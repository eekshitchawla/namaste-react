import React from "react";

const MindItem = () => {
  return (
    <>
      {(window.location.href = "/")}
      {alert("Page/API didn't respond. Returning to Main Menu!")}
    </>
  );
};

export default MindItem;
